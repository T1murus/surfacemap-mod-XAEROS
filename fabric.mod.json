{
  "schemaVersion": 1,
  "id": "surfacemap_client",
  "version": "${version}",
  "name": "SurfaceMap Client",
  "description": "Locks Xaero's World Map to surface-only mode as required by server",
  "authors": ["ServerAdmin"],
  "environment": "client",
  "entrypoints": {
    "client": ["com.surfacemap.SurfaceMapClient"]
  },
  "depends": {
    "fabricloader": ">=0.16.0",
    "fabric-api": "*",
    "minecraft": "~1.21.1"
  }
}
